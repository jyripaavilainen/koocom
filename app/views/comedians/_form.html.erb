<%= simple_form_for(@comedian) do |f| %>
  <% if @comedian.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comedian.errors.count, "error") %> virhe tapahtui ja koomikkoa ei tallennettu:</h2>

      <ul>
      <% @comedian.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :stagename, label: 'Esiintymisnimi' %>
  <%= f.input :website_url, label: 'Kotisivun osoite (http://...)' %>
  <%= f.input :contact_email, label: 'Sähköpostiosoite' %>
  <%= f.input :description, label: 'Kuvaus', :input_html => { :rows => 10 } %>
  <%= f.input :profile_photo_url, label: 'Kuvan url-osoite' %>
  <%= f.input :slug, label: 'Profiilin url-osoitteen loppuosa' %>
  <%= f.input :youtube_video_id, label: 'Youtube-videon id', required: false %>

  <div class="form-actions">
    <%= f.button :submit, "Tallenna" %>
  </div>
<% end %>
